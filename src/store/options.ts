import { local } from './utils';

export const title = local('title', '');

export const difficulty = local('difficulty', '');

export const rate = local('rate', 1);

export const musicVolume = local('musicVolume', 1);

export const fxVolume = local('fxVolume', 1);
